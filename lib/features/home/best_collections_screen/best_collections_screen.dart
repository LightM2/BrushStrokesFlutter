import 'package:flutter/material.dart';

class BestCollectionsScreen extends StatelessWidget {
  const BestCollectionsScreen({super.key});

  static const routeName = '/best_collections';

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Text('Best Collections'),
    );
  }
}
